import './SearchBar.css'

const SearchBar = () => {
    return (
        <div id='search-bar'>
            <input type="text" />
            <button>🔍</button>
        </div>
    )
}

export default SearchBar